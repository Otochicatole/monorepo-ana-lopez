version: "3.8"

services:
  postgres:
    image: postgres:15
    container_name: db-ana
    restart: always
    environment:
      POSTGRES_DB: db-ana
      POSTGRES_USER: db-ana
      POSTGRES_PASSWORD: ender005asd
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5433:5432"

volumes:
  pgdata:
